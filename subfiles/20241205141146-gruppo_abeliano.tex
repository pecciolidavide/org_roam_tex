% Intended LaTeX compiler: pdflatex
\documentclass[../main]{subfiles}


\begin{document}

\section{Definizione}
\label{sec:org5ddfdb9}
Un gruppo è un \href{20250130104331-insieme_mk.org}{insieme} \(G\) dotato di una \href{20250206171120-operazione_su_una_classe_mk.org}{operazione} \(*:G\times G\to G\) tale che
\begin{itemize}
\item dati \(a,b,c \in G\): \((a*b)*c = a*(b*c)\);
\item esiste \(e \in G\) tale che per ogni \(a \in G\): \(a*e=e*a=a\);
\item per ogni \(a \in G\) esiste \(a' \in G\) tale che \(a*a'=a'*a=e\).
\end{itemize}

Si denota con \(\langle G, *\rangle\) oppure con \(\langle G,*,e\rangle\).
\end{document}
